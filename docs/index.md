# pyspark-schemas

`pyspark-schemas` is a Python library that provides schema safety for your Spark jobs. It allows you to define your data schemas using Python dataclasses and automatically generates the corresponding Spark schemas, ensuring that your data transformations are type-safe and consistent.

## Guides

- [The `@transform` decorator](transform.md)
